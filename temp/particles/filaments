search synth, filter, render, points, mixer

let osc1 = osc(type: oscKind.sawInv)

perlin(
  scale: 87.06,
  dimensions: 3,
  seed: 5
)
  .seamless(repeat: 1)
  .adjust(
    rotation: osc1,
    hueRange: 200,
    contrast: 1
  )
  .subchain(name: "flow field particles", id: "p0wu") {
    .pointsEmit(attrition: 2.5)
    .hydraulic(stride: 83)
    .pointsRender(
      density: 100,
      intensity: 49.68,
      inputIntensity: 0
    )
  }
  .adjust(
    mode: hsv,
    rotation: -43.49,
    hueRange: 15.22
  )
  .subchain(name: "lens effects", id: "dwdx") {
    .temporalAberration()
    .bloom(softKnee: 0.16, taps: 15)
    .lens(displacement: -0.5)
    .vignette()
  }
  .adjust(brightness: 2.03, contrast: 0.55)
  .write(o0)

render(o0)
